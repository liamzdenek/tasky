'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = decorateFunction;

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function decorateFunction(type, fn) {

  var decorated = function decorated() {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    type.assertParams.apply(type, _toConsumableArray(args));
    var result = fn.apply(this, args);
    type.assertReturn(result);
    return result;
  };

  Object.defineProperties(decorated, {
    name: {
      value: fn.name || 'anonymous'
    },
    length: {
      value: fn.length
    }
  });

  return decorated;
}