'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = decorateGeneratorFunction;

var _invariant = require('./invariant');

var _invariant2 = _interopRequireDefault(_invariant);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function decorateGeneratorFunction(type, fn) {

  // const generatorType = type.returnType;

  var decorated = function* decorated() {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    type.assertParams.apply(type, _toConsumableArray(args));
    var gen = fn.apply(this, args);
    var next = void 0;
    while (!(next = gen.next()).done) {
      // eslint-disable-line
      var value = next.value;
      value;
      yield value;
    }
    (0, _invariant2.default)(gen.done, 'Generator has exhausted.');
    var result = next.value;
    result;
    //generatorType.assertReturn(result);

    // Ignore
    return result;
  };

  Object.defineProperties(decorated, {
    name: {
      value: fn.name || 'anonymous'
    },
    length: {
      value: fn.length
    }
  });

  return decorated;
}